server:
  port: 5000

spring:
  application:
    name: bold-connect

# Information that shows up in the /info endpoint
info:
  app:
    name: bold-connect
    version: 1.0.0


logging:
  level:
    ROOT: INFO
    com.poc.boldconnect: DEBUG
    org.springframework:
      web: DEBUG
      beans: ERROR
      boot: ERROR
    com.netflix:
      discovery: ERROR
      loadbalancer : ERROR

